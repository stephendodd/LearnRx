/*
 Exercise 4: Implement map
    Implement map function to make projection easier.
    Map accept a projection function to be applied to each of the item in the array, and returns the projected array.

    Example: [1,2,3].map(x => x + 1) to be [2,3,4]
 */
function exercise () {
    Array.prototype.map = function(projectionFunction) {
        let sourceArray = this,
            results = [];

        /*
         Add your code here
         */

        return results;
    }
}

export default exercise;

/*
 Exercise 23: Construct a tree from Arrays
 */
function exercise (worlds, heroes, superPowers, avatars) {
    let heroesInTheWorld;
    /*
     Add your code here,
      Try to construct an array with item like:
      [{world: 'DC', heroes: [{id, name, identity, superPower, avatars}...]}...]
     */
    return heroesInTheWorld;
}

export default exercise;
